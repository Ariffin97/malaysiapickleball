<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Executive Committee - Malaysia Pickleball Association</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/styles.css">
  <link rel="icon" href="/favicon.ico" type="image/x-icon">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
  <style>
    body {
      font-family: 'Inter', 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
      background: #f8fafc;
      color: #1a202c;
      line-height: 1.6;
    }
    
    .content-section {
      background: transparent;
      padding-top: 8rem;
      margin-top: 0;
      min-height: 100vh;
    }
    
    .container {
      max-width: 1200px;
      margin: 0 auto;
      padding: 0 1.5rem;
    }
    
    .page-header {
      text-align: center;
      margin-bottom: 3rem;
      padding-bottom: 2rem;
      border-bottom: 1px solid #e2e8f0;
    }
    
    .page-title {
      font-size: 2.75rem;
      font-weight: 800;
      color: #1a202c;
      margin-bottom: 1rem;
      letter-spacing: -0.02em;
    }
    
    .page-subtitle {
      font-size: 1.1rem;
      color: #64748b;
      max-width: 600px;
      margin: 0 auto;
      line-height: 1.7;
    }
    
    /* Navigation Buttons */
    .nav-buttons {
      display: flex;
      justify-content: center;
      gap: 1rem;
      margin-top: 2rem;
    }
    
    .nav-btn {
      padding: 0.75rem 1.5rem;
      border: 2px solid #e2e8f0;
      border-radius: 8px;
      background: white;
      color: #64748b;
      font-weight: 600;
      font-size: 0.9rem;
      cursor: pointer;
      transition: all 0.3s ease;
      display: flex;
      align-items: center;
    }
    
    .nav-btn:hover {
      transform: translateY(-2px);
      box-shadow: 0 4px 14px rgba(0, 0, 0, 0.1);
      border-color: #3b82f6;
      color: #3b82f6;
    }
    
    .nav-btn.active {
      background: linear-gradient(135deg, #3b82f6 0%, #1d4ed8 100%);
      color: white;
      border-color: #3b82f6;
      box-shadow: 0 4px 14px rgba(59, 130, 246, 0.3);
    }
    
    /* Section Styles */
    .section-title {
      font-size: 1.5rem;
      font-weight: 700;
      color: #1a202c;
      margin-bottom: 1.5rem;
      text-align: center;
      position: relative;
    }
    
    .section-title::after {
      content: '';
      width: 60px;
      height: 3px;
      background: linear-gradient(90deg, #3b82f6, #1d4ed8);
      position: absolute;
      bottom: -8px;
      left: 50%;
      transform: translateX(-50%);
      border-radius: 2px;
    }
    
    .committee-grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
      gap: 2rem;
      margin-bottom: 3rem;
    }
    
    .member-card {
      text-align: center;
      padding: 1rem;
    }
    
    .member-photo {
      width: 180px;
      height: 240px;
      border-radius: 12px;
      margin: 0 auto 1.5rem;
      overflow: hidden;
      position: relative;
    }
    
    .member-photo img {
      width: 100%;
      height: 100%;
      object-fit: cover;
    }
    
    .member-photo-placeholder {
      width: 100%;
      height: 100%;
      background: linear-gradient(135deg, #e2e8f0 0%, #cbd5e0 100%);
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 2.5rem;
      color: #94a3b8;
    }
    
    .member-title {
      font-size: 0.875rem;
      font-weight: 600;
      color: #3b82f6;
      text-transform: uppercase;
      letter-spacing: 0.1em;
      margin-bottom: 0.5rem;
    }
    
    .member-name {
      font-size: 1.25rem;
      font-weight: 700;
      color: #1a202c;
      margin-bottom: 1rem;
      line-height: 1.3;
    }
    
    /* Past Presidents Table */
    .past-presidents-table {
      width: 100%;
      background: white;
      border-radius: 12px;
      box-shadow: 0 4px 20px rgba(0, 0, 0, 0.08);
      overflow: hidden;
      border-collapse: collapse;
      margin-top: 2rem;
    }
    
    .past-presidents-table th {
      background: linear-gradient(135deg, #3b82f6 0%, #1d4ed8 100%);
      color: white;
      padding: 1rem;
      text-align: center;
      font-weight: 600;
      font-size: 0.9rem;
      text-transform: uppercase;
      letter-spacing: 0.05em;
    }
    
    .past-presidents-table th:nth-child(1) {
      width: 160px;
    }
    
    .past-presidents-table th:nth-child(2) {
      width: 200px;
    }
    
    .past-presidents-table th:nth-child(3) {
      width: 150px;
    }
    
    .past-presidents-table th:nth-child(4) {
      width: auto;
    }
    
    .past-presidents-table td {
      padding: 1.5rem 1rem;
      border-bottom: 1px solid #e2e8f0;
      vertical-align: top;
      text-align: center;
    }
    
    .past-presidents-table td:nth-child(1) {
      width: 160px;
    }
    
    .past-presidents-table td:nth-child(2) {
      width: 200px;
    }
    
    .past-presidents-table td:nth-child(3) {
      width: 150px;
    }
    
    .past-presidents-table td:nth-child(4) {
      width: auto;
      text-align: left;
    }
    
    .past-presidents-table tbody tr:last-child td {
      border-bottom: none;
    }
    
    .past-presidents-table tbody tr:hover {
      background-color: #f8fafc;
    }
    
    .president-photo {
      width: 120px;
      height: 150px;
      border-radius: 8px;
      overflow: hidden;
      border: 2px solid #e2e8f0;
      margin: 0 auto;
    }
    
    .president-photo img,
    .president-photo-img {
      width: 100%;
      height: 100%;
      object-fit: cover;
    }
    
    .president-photo-placeholder {
      width: 100%;
      height: 100%;
      background: linear-gradient(135deg, #e2e8f0 0%, #cbd5e0 100%);
      display: flex;
      align-items: center;
      justify-content: center;
      font-size: 2rem;
      color: #94a3b8;
    }
    
    .president-name {
      font-weight: 700;
      color: #1a202c;
      font-size: 1rem;
    }
    
    .president-term {
      font-weight: 600;
      color: #3b82f6;
      font-size: 0.9rem;
      text-align: center;
    }
    
    .president-contribution {
      color: #64748b;
      line-height: 1.5;
      font-size: 0.85rem;
    }
    
    .back-button {
      background: linear-gradient(135deg, #4b5563 0%, #374151 100%);
      color: white;
      padding: 1rem 2rem;
      border-radius: 12px;
      text-decoration: none;
      display: inline-flex;
      align-items: center;
      font-weight: 600;
      font-size: 1rem;
      transition: all 0.3s ease;
      box-shadow: 0 4px 14px rgba(75, 85, 99, 0.3);
      border: 2px solid transparent;
    }
    
    .back-button:hover {
      transform: translateY(-2px);
      box-shadow: 0 8px 20px rgba(75, 85, 99, 0.4);
      text-decoration: none;
      color: white;
      border-color: rgba(255, 255, 255, 0.2);
    }
    
    /* Hide sections by default */
    .content-section-hidden {
      display: none;
    }
    
    @media (max-width: 768px) {
      .page-title {
        font-size: 2.25rem;
      }
      
      .committee-grid {
        grid-template-columns: 1fr;
        gap: 1.5rem;
      }
      
      .nav-buttons {
        flex-direction: column;
        align-items: center;
      }
      
      .nav-btn {
        width: 100%;
        max-width: 300px;
        justify-content: center;
      }
      
      .past-presidents-table {
        font-size: 0.8rem;
      }
      
      .past-presidents-table th,
      .past-presidents-table td {
        padding: 0.75rem 0.5rem;
      }
      
      .president-photo {
        width: 90px;
        height: 110px;
      }
    }
  </style>
</head>
<body>
  <%- include('../partials/header') %>

  <section class="content-section">
    <div class="container">
      <!-- Page Header -->
      <div class="page-header">
        <h1 class="page-title">Executive Committee</h1>
        <p class="page-subtitle">Meet the dedicated leaders driving Malaysia Pickleball Association forward with passion, expertise, and commitment to excellence in sports development.</p>
        
        <!-- Navigation Buttons -->
        <div class="nav-buttons">
          <button onclick="showExecutive()" class="nav-btn active" id="executive-btn">
            <i class="fas fa-users" style="margin-right: 0.5rem;"></i>
            Executive Committee
          </button>
          <button onclick="showPastPresidents()" class="nav-btn" id="past-btn">
            <i class="fas fa-crown" style="margin-right: 0.5rem;"></i>
            Past Presidents
          </button>
        </div>
      </div>

      <!-- Executive Committee Section -->
      <div id="executive-section">
        <!-- President Section -->
        <div style="margin-bottom: 4rem;">
          <h2 class="section-title">Leadership</h2>
          <div class="committee-grid">
            <div class="member-card" style="grid-column: 1 / -1; max-width: 400px; margin: 0 auto;">
              <div class="member-photo">
                <% if (orgChartData?.president?.photo && orgChartData.president.photo !== '👨‍💼' && orgChartData.president.photo !== '👩‍💼' && orgChartData.president.photo !== null) { %>
                  <img src="<%= orgChartData.president.photo %>" alt="<%= orgChartData.president.name %>">
                <% } else { %>
                  <div class="member-photo-placeholder">
                    <i class="fas fa-user"></i>
                  </div>
                <% } %>
              </div>
              <div class="member-title">President</div>
              <div class="member-name"><%= orgChartData?.president?.name || 'Datuk Ahmad Zulkarnain' %></div>
            </div>
          </div>
        </div>

        <!-- Vice Presidents Section -->
        <div style="margin-bottom: 4rem;">
          <h2 class="section-title">Vice Presidents</h2>
          <div class="committee-grid">
            <% if (orgChartData && orgChartData.vicePresidents) { %>
              <% orgChartData.vicePresidents.forEach(vp => { %>
                <div class="member-card">
                  <div class="member-photo">
                    <% if (vp.photo && vp.photo !== '👨‍💼' && vp.photo !== '👩‍💼' && vp.photo !== null) { %>
                      <img src="<%= vp.photo %>" alt="<%= vp.name %>">
                    <% } else { %>
                      <div class="member-photo-placeholder">
                        <i class="fas fa-user"></i>
                      </div>
                    <% } %>
                  </div>
                  <div class="member-title">Vice President</div>
                  <div class="member-name"><%= vp.name %></div>
                </div>
              <% }) %>
            <% } else { %>
              <div class="member-card">
                <div class="member-photo">
                  <div class="member-photo-placeholder">
                    <i class="fas fa-user"></i>
                  </div>
                </div>
                <div class="member-title">Vice President</div>
                <div class="member-name">Dato' Dr. Lim Chong</div>
              </div>
              <div class="member-card">
                <div class="member-photo">
                  <div class="member-photo-placeholder">
                    <i class="fas fa-user"></i>
                  </div>
                </div>
                <div class="member-title">Vice President</div>
                <div class="member-name">Datuk Seri Wong Lee</div>
              </div>
            <% } %>
          </div>
        </div>

        <!-- Executive Committee Section -->
        <div style="margin-bottom: 4rem;">
          <h2 class="section-title">Executive Committee</h2>
          <div class="committee-grid">
            <div class="member-card">
              <div class="member-photo">
                <% if (orgChartData?.secretary?.photo && orgChartData.secretary.photo !== '👨‍💼' && orgChartData.secretary.photo !== '👩‍💼' && orgChartData.secretary.photo !== null) { %>
                  <img src="<%= orgChartData.secretary.photo %>" alt="<%= orgChartData.secretary.name %>">
                <% } else { %>
                  <div class="member-photo-placeholder">
                    <i class="fas fa-user"></i>
                  </div>
                <% } %>
              </div>
              <div class="member-title">Secretary</div>
              <div class="member-name"><%= orgChartData?.secretary?.name || 'Mohd Azlan bin Abdullah' %></div>
            </div>
            
            <div class="member-card">
              <div class="member-photo">
                <% if (orgChartData?.treasurer?.photo && orgChartData.treasurer.photo !== '👨‍💼' && orgChartData.treasurer.photo !== '👩‍💼' && orgChartData.treasurer.photo !== null) { %>
                  <img src="<%= orgChartData.treasurer.photo %>" alt="<%= orgChartData.treasurer.name %>">
                <% } else { %>
                  <div class="member-photo-placeholder">
                    <i class="fas fa-user"></i>
                  </div>
                <% } %>
              </div>
              <div class="member-title">Treasurer</div>
              <div class="member-name"><%= orgChartData?.treasurer?.name || 'Wong Mei Ling' %></div>
            </div>
          </div>
        </div>
      </div>

      <!-- Past Presidents Section -->
      <div id="past-section" class="content-section-hidden">
        <h2 class="section-title">Past Presidents</h2>
        
        <table class="past-presidents-table">
          <thead>
            <tr>
              <th>Image</th>
              <th>Name</th>
              <th>Years of Service</th>
              <th>Contribution</th>
            </tr>
          </thead>
          <tbody>
            <% if (pastPresidents && pastPresidents.length > 0) { %>
              <% pastPresidents.forEach(president => { %>
                <tr>
                  <td>
                    <div class="president-photo">
                      <% if (president.image) { %>
                        <img src="<%= president.image %>" alt="<%= president.imageAlt || president.name %>" class="president-photo-img">
                      <% } else { %>
                        <div class="president-photo-placeholder">
                          <i class="fas fa-user"></i>
                        </div>
                      <% } %>
                    </div>
                  </td>
                  <td>
                    <div class="president-name"><%= president.name %></div>
                  </td>
                  <td>
                    <div class="president-term"><%= president.startYear %> - <%= president.endYear %></div>
                  </td>
                  <td>
                    <div class="president-contribution">
                      <%= president.contribution %>
                      <% if (president.achievements && president.achievements.length > 0) { %>
                        <div class="president-achievements">
                          <strong>Key Achievements:</strong>
                          <ul>
                            <% president.achievements.forEach(achievement => { %>
                              <li>• <%= achievement %></li>
                            <% }); %>
                          </ul>
                        </div>
                      <% } %>
                    </div>
                  </td>
                </tr>
              <% }); %>
            <% } else { %>
              <tr>
                <td colspan="4" style="text-align: center; padding: 2rem;">
                  <div style="color: #6b7280;">
                    <i class="fas fa-users" style="font-size: 2rem; margin-bottom: 1rem; display: block;"></i>
                    <p>No past presidents data available.</p>
                  </div>
                </td>
              </tr>
            <% } %>
          </tbody>
        </table>
      </div>

      <!-- Back Button -->
      <div style="text-align: center; margin-top: 3rem;">
        <a href="/" class="back-button">
          <i class="fas fa-arrow-left" style="margin-right: 0.5rem;"></i>
          Kembali ke Laman Utama
        </a>
      </div>
    </div>
  </section>

  <%- include('../partials/footer') %>

  <script src="/js/script.js"></script>
  <script>
    function showExecutive() {
      // Hide past presidents
      document.getElementById('past-section').classList.add('content-section-hidden');
      // Show executive
      document.getElementById('executive-section').classList.remove('content-section-hidden');
      
      // Update button states
      document.getElementById('executive-btn').classList.add('active');
      document.getElementById('past-btn').classList.remove('active');
    }
    
    function showPastPresidents() {
      // Hide executive
      document.getElementById('executive-section').classList.add('content-section-hidden');
      // Show past presidents
      document.getElementById('past-section').classList.remove('content-section-hidden');
      
      // Update button states
      document.getElementById('past-btn').classList.add('active');
      document.getElementById('executive-btn').classList.remove('active');
    }
    
    // Initialize - show executive by default
    document.addEventListener('DOMContentLoaded', function() {
      showExecutive();
    });
  </script>
</body>
</html>