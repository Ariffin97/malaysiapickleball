<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Player Registration - Malaysia Pickleball Association</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/styles.css">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
  <style>
    .form-card {
      backdrop-filter: blur(10px);
      background: rgba(255, 255, 255, 0.95);
    }
    .input-group {
      position: relative;
    }
    .input-group input,
    .input-group select,
    .input-group textarea {
      transition: all 0.3s ease;
    }
    .input-group input:focus,
    .input-group select:focus,
    .input-group textarea:focus {
      transform: translateY(-2px);
      box-shadow: 0 8px 25px rgba(0,0,0,0.1);
    }
    .profile-preview {
      width: 120px;
      height: 120px;
      border-radius: 50%;
      object-fit: cover;
      border: 4px solid #667eea;
    }
    .upload-area {
      border: 2px dashed #cbd5e1;
      border-radius: 12px;
      transition: all 0.3s ease;
    }
    .upload-area:hover {
      border-color: #667eea;
      background: rgba(102, 126, 234, 0.05);
    }
    .upload-area.drag-over {
      border-color: #667eea;
      background: rgba(102, 126, 234, 0.1);
    }
  </style>
</head>
<%
  const defaultBg = backgroundImage || '/images/defaultbg.png';
%>
<body class="bg-gray-100 bg-cover bg-center bg-fixed min-h-screen" style="background-image: url('<%= defaultBg %>')">

  <main class="py-12">
    <div class="container mx-auto px-4 max-w-4xl">
      
      <!-- Registration Form -->
      <div class="form-card rounded-2xl shadow-2xl p-8 mb-8">
        <div class="text-center mb-8">
          <h2 class="text-3xl font-bold text-gray-800 mb-2">
            <i class="fas fa-clipboard-list text-blue-600 mr-3"></i>
            Registration Form
          </h2>
          <p class="text-gray-600">Please fill in all required information to complete your registration</p>
        </div>

        <% if (typeof error !== 'undefined' && error) { %>
          <div class="bg-red-100 border border-red-400 text-red-700 px-4 py-3 rounded mb-6">
            <i class="fas fa-exclamation-triangle mr-2"></i>
            <%= error %>
          </div>
        <% } %>

        <% if (typeof success !== 'undefined' && success) { %>
          <div class="bg-green-100 border border-green-400 text-green-700 px-4 py-3 rounded mb-6">
            <i class="fas fa-check-circle mr-2"></i>
            <%= success %>
          </div>
        <% } %>

        <form action="/player/register" method="POST" enctype="multipart/form-data" id="registrationForm">
          <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
            
            <!-- Personal Information -->
            <div class="md:col-span-2">
              <h3 class="text-xl font-semibold text-gray-800 mb-4 flex items-center">
                <i class="fas fa-user text-blue-600 mr-2"></i>
                Personal Information
              </h3>
            </div>

            <!-- Full Name -->
            <div class="input-group">
              <label for="fullName" class="block text-sm font-medium text-gray-700 mb-2">
                <i class="fas fa-user mr-2"></i>Full Name *
              </label>
              <input type="text" id="fullName" name="fullName" required
                     class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500"
                     placeholder="Enter your full name as per IC">
            </div>

            <!-- Identity Card Number -->
            <div class="input-group">
              <label for="icNumber" class="block text-sm font-medium text-gray-700 mb-2">
                <i class="fas fa-id-card mr-2"></i>Identity Card Number *
              </label>
              <input type="text" id="icNumber" name="icNumber" required
                     pattern="^[0-9]{6}-[0-9]{2}-[0-9]{4}$"
                     class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500"
                     placeholder="123456-78-9012"
                     maxlength="14">
              <div id="icValidation" class="mt-1 text-xs hidden">
                <div id="icLoading" class="text-blue-600 hidden">
                  <i class="fas fa-spinner fa-spin mr-1"></i>Checking availability...
                </div>
                <div id="icAvailable" class="text-green-600 hidden">
                  <i class="fas fa-check-circle mr-1"></i>IC number is available for registration
                </div>
                <div id="icUnavailable" class="text-red-600 hidden">
                  <i class="fas fa-times-circle mr-1"></i><span id="icErrorMessage"></span>
                </div>
              </div>
              <p class="text-xs text-gray-500 mt-1">Enter IC number with or without dashes - will auto-format to 123456-78-9012. Each IC number can only be registered once.</p>
            </div>

            <!-- Age -->
            <div class="input-group">
              <label for="age" class="block text-sm font-medium text-gray-700 mb-2">
                <i class="fas fa-calendar-alt mr-2"></i>Age *
              </label>
              <input type="number" id="age" name="age" required min="12" max="100"
                     class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500"
                     placeholder="25">
            </div>

            <!-- Phone Number -->
            <div class="input-group">
              <label for="phoneNumber" class="block text-sm font-medium text-gray-700 mb-2">
                <i class="fas fa-phone mr-2"></i>Phone Number *
              </label>
              <input type="tel" id="phoneNumber" name="phoneNumber" required
                     pattern="[0-9+\-\s()]*"
                     class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500"
                     placeholder="+60123456789">
            </div>

            <!-- Email -->
            <div class="input-group md:col-span-2">
              <label for="email" class="block text-sm font-medium text-gray-700 mb-2">
                <i class="fas fa-envelope mr-2"></i>Email Address *
              </label>
              <input type="email" id="email" name="email" required
                     class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500"
                     placeholder="your.email@example.com">
            </div>

            <!-- Address -->
            <div class="input-group md:col-span-2">
              <label for="address" class="block text-sm font-medium text-gray-700 mb-2">
                <i class="fas fa-map-marker-alt mr-2"></i>Address *
              </label>
              <textarea id="address" name="address" required rows="3"
                        class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500"
                        placeholder="Enter your complete address"></textarea>
            </div>

            <!-- City -->
            <div class="input-group">
              <label for="city" class="block text-sm font-medium text-gray-700 mb-2">
                <i class="fas fa-city mr-2"></i>City *
              </label>
              <select id="city" name="city" required
                      class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500 bg-white">
                <option value="">Select your city</option>
                
                <!-- Kuala Lumpur & Selangor -->
                <optgroup label="Kuala Lumpur & Selangor">
                  <option value="Kuala Lumpur">Kuala Lumpur</option>
                  <option value="Petaling Jaya">Petaling Jaya</option>
                  <option value="Shah Alam">Shah Alam</option>
                  <option value="Subang Jaya">Subang Jaya</option>
                  <option value="Klang">Klang</option>
                  <option value="Ampang">Ampang</option>
                  <option value="Cheras">Cheras</option>
                  <option value="Kajang">Kajang</option>
                  <option value="Selayang">Selayang</option>
                  <option value="Rawang">Rawang</option>
                  <option value="Sepang">Sepang</option>
                  <option value="Putrajaya">Putrajaya</option>
                  <option value="Cyberjaya">Cyberjaya</option>
                </optgroup>
                
                <!-- Johor -->
                <optgroup label="Johor">
                  <option value="Johor Bahru">Johor Bahru</option>
                  <option value="Skudai">Skudai</option>
                  <option value="Iskandar Puteri">Iskandar Puteri</option>
                  <option value="Muar">Muar</option>
                  <option value="Batu Pahat">Batu Pahat</option>
                  <option value="Kluang">Kluang</option>
                  <option value="Pontian">Pontian</option>
                  <option value="Segamat">Segamat</option>
                  <option value="Kota Tinggi">Kota Tinggi</option>
                </optgroup>
                
                <!-- Penang -->
                <optgroup label="Penang">
                  <option value="George Town">George Town</option>
                  <option value="Butterworth">Butterworth</option>
                  <option value="Bukit Mertajam">Bukit Mertajam</option>
                  <option value="Nibong Tebal">Nibong Tebal</option>
                  <option value="Permatang Pauh">Permatang Pauh</option>
                </optgroup>
                
                <!-- Perak -->
                <optgroup label="Perak">
                  <option value="Ipoh">Ipoh</option>
                  <option value="Taiping">Taiping</option>
                  <option value="Teluk Intan">Teluk Intan</option>
                  <option value="Kampar">Kampar</option>
                  <option value="Kuala Kangsar">Kuala Kangsar</option>
                  <option value="Parit Buntar">Parit Buntar</option>
                  <option value="Sitiawan">Sitiawan</option>
                </optgroup>
                
                <!-- Kedah -->
                <optgroup label="Kedah">
                  <option value="Alor Setar">Alor Setar</option>
                  <option value="Sungai Petani">Sungai Petani</option>
                  <option value="Kulim">Kulim</option>
                  <option value="Langkawi">Langkawi</option>
                  <option value="Kubang Pasu">Kubang Pasu</option>
                </optgroup>
                
                <!-- Negeri Sembilan -->
                <optgroup label="Negeri Sembilan">
                  <option value="Seremban">Seremban</option>
                  <option value="Port Dickson">Port Dickson</option>
                  <option value="Nilai">Nilai</option>
                  <option value="Bahau">Bahau</option>
                  <option value="Tampin">Tampin</option>
                </optgroup>
                
                <!-- Melaka -->
                <optgroup label="Melaka">
                  <option value="Melaka City">Melaka City</option>
                  <option value="Alor Gajah">Alor Gajah</option>
                  <option value="Jasin">Jasin</option>
                  <option value="Merlimau">Merlimau</option>
                </optgroup>
                
                <!-- Pahang -->
                <optgroup label="Pahang">
                  <option value="Kuantan">Kuantan</option>
                  <option value="Temerloh">Temerloh</option>
                  <option value="Bentong">Bentong</option>
                  <option value="Raub">Raub</option>
                  <option value="Jerantut">Jerantut</option>
                  <option value="Pekan">Pekan</option>
                  <option value="Kuala Lipis">Kuala Lipis</option>
                </optgroup>
                
                <!-- Terengganu -->
                <optgroup label="Terengganu">
                  <option value="Kuala Terengganu">Kuala Terengganu</option>
                  <option value="Kemaman">Kemaman</option>
                  <option value="Dungun">Dungun</option>
                  <option value="Marang">Marang</option>
                  <option value="Besut">Besut</option>
                </optgroup>
                
                <!-- Kelantan -->
                <optgroup label="Kelantan">
                  <option value="Kota Bharu">Kota Bharu</option>
                  <option value="Tanah Merah">Tanah Merah</option>
                  <option value="Machang">Machang</option>
                  <option value="Pasir Mas">Pasir Mas</option>
                  <option value="Tumpat">Tumpat</option>
                  <option value="Bachok">Bachok</option>
                </optgroup>
                
                <!-- Perlis -->
                <optgroup label="Perlis">
                  <option value="Kangar">Kangar</option>
                  <option value="Arau">Arau</option>
                  <option value="Padang Besar">Padang Besar</option>
                </optgroup>
                
                <!-- Sabah -->
                <optgroup label="Sabah">
                  <option value="Kota Kinabalu">Kota Kinabalu</option>
                  <option value="Sandakan">Sandakan</option>
                  <option value="Tawau">Tawau</option>
                  <option value="Lahad Datu">Lahad Datu</option>
                  <option value="Keningau">Keningau</option>
                  <option value="Semporna">Semporna</option>
                  <option value="Kudat">Kudat</option>
                </optgroup>
                
                <!-- Sarawak -->
                <optgroup label="Sarawak">
                  <option value="Kuching">Kuching</option>
                  <option value="Miri">Miri</option>
                  <option value="Sibu">Sibu</option>
                  <option value="Bintulu">Bintulu</option>
                  <option value="Limbang">Limbang</option>
                  <option value="Sarikei">Sarikei</option>
                  <option value="Sri Aman">Sri Aman</option>
                  <option value="Kapit">Kapit</option>
                </optgroup>
                
                <!-- Other -->
                <optgroup label="Other">
                  <option value="Labuan">Labuan</option>
                  <option value="Other">Other (Please specify in address)</option>
                </optgroup>
              </select>
            </div>

            <!-- Postcode -->
            <div class="input-group">
              <label for="postcode" class="block text-sm font-medium text-gray-700 mb-2">
                <i class="fas fa-mail-bulk mr-2"></i>Postcode *
              </label>
              <input type="text" id="postcode" name="postcode" required pattern="[0-9]{5}"
                     class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500"
                     placeholder="Enter your postcode (5 digits)" maxlength="5">
            </div>

            <!-- Account Information -->
            <div class="md:col-span-2 mt-6">
              <h3 class="text-xl font-semibold text-gray-800 mb-4 flex items-center">
                <i class="fas fa-key text-green-600 mr-2"></i>
                Account Information
              </h3>
            </div>

            <!-- Username -->
            <div class="input-group">
              <label for="username" class="block text-sm font-medium text-gray-700 mb-2">
                <i class="fas fa-user-circle mr-2"></i>Username *
              </label>
              <input type="text" id="username" name="username" required
                     pattern="[a-zA-Z0-9_]{3,20}"
                     class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500"
                     placeholder="Choose a unique username">
              <p class="text-xs text-gray-500 mt-1">3-20 characters, letters, numbers, and underscores only</p>
            </div>

            <!-- Password -->
            <div class="input-group">
              <label for="password" class="block text-sm font-medium text-gray-700 mb-2">
                <i class="fas fa-lock mr-2"></i>Password *
              </label>
              <input type="password" id="password" name="password" required
                     minlength="6"
                     class="w-full px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-blue-500"
                     placeholder="Create a strong password">
              <p class="text-xs text-gray-500 mt-1">Minimum 6 characters</p>
            </div>

            <!-- Profile Picture -->
            <div class="md:col-span-2 mt-6">
              <h3 class="text-xl font-semibold text-gray-800 mb-4 flex items-center">
                <i class="fas fa-camera text-purple-600 mr-2"></i>
                Profile Picture
              </h3>
              
              <div class="flex flex-col md:flex-row items-start md:items-center gap-6">
                <!-- Preview -->
                <div class="text-center">
                  <img id="profilePreview" src="/images/default-avatar.png" alt="Profile Preview" 
                       class="profile-preview mx-auto mb-2" style="display: none;">
                  <div id="defaultAvatar" class="profile-preview mx-auto mb-2 bg-gray-200 flex items-center justify-center">
                    <i class="fas fa-user text-4xl text-gray-400"></i>
                  </div>
                  <p class="text-xs text-gray-500">Preview</p>
                </div>

                <!-- Upload Area -->
                <div class="flex-1">
                  <div class="upload-area p-6 text-center cursor-pointer" onclick="triggerFileInput()">
                    <input type="file" id="profilePicture" name="profilePicture" accept="image/*" class="hidden">
                    <div class="mb-4">
                      <i class="fas fa-cloud-upload-alt text-3xl text-gray-400 mb-2"></i>
                      <p class="text-gray-600 font-medium">Click to upload or drag and drop</p>
                      <p class="text-sm text-gray-500">PNG, JPG, JPEG up to 5MB</p>
                    </div>
                    <label for="profilePicture" id="chooseFileBtn" class="bg-blue-600 text-white px-6 py-2 rounded-lg hover:bg-blue-700 transition-colors cursor-pointer inline-block">
                      <i class="fas fa-upload mr-2"></i>Choose File
                    </label>
                  </div>
                </div>
              </div>
            </div>

            <!-- Terms and Conditions -->
            <div class="md:col-span-2 mt-6">
              <div class="bg-gray-50 p-4 rounded-lg">
                <label class="flex items-start">
                  <input type="checkbox" id="terms" name="terms" required class="mt-1 mr-3">
                  <span class="text-sm text-gray-700">
                    I agree to the <a href="#" id="termsLink" class="text-blue-600 hover:underline">Terms and Conditions</a> 
                    and <a href="#" id="privacyLink" class="text-blue-600 hover:underline">Privacy Policy</a> of Malaysia Pickleball Association. 
                    I understand that my registration is subject to admin approval and I will receive a notification once approved.
                  </span>
                </label>
              </div>
            </div>

            <!-- Submit Button -->
            <div class="md:col-span-2 mt-8">
              <button type="submit" id="submitBtn"
                      class="w-full bg-gradient-to-r from-blue-600 to-purple-600 text-white py-4 px-8 rounded-xl font-bold text-lg hover:from-blue-700 hover:to-purple-700 transition-all duration-300 transform hover:scale-105 shadow-lg">
                <i class="fas fa-paper-plane mr-3"></i>
                Submit Registration
              </button>
              <p class="text-center text-sm text-gray-500 mt-2">
                Your registration will be reviewed by our admin team within 24-48 hours
              </p>
            </div>

          </div>
        </form>
      </div>

      <!-- Benefits Section -->
      <div class="form-card rounded-2xl shadow-xl p-8">
        <h3 class="text-2xl font-bold text-gray-800 mb-6 text-center">
          <i class="fas fa-star text-yellow-500 mr-2"></i>
          Member Benefits
        </h3>
        <div class="grid grid-cols-1 md:grid-cols-3 gap-6">
          <div class="text-center">
            <div class="bg-blue-100 w-16 h-16 rounded-full flex items-center justify-center mx-auto mb-4">
              <i class="fas fa-id-badge text-2xl text-blue-600"></i>
            </div>
            <h4 class="font-semibold text-gray-800 mb-2">Unique Player ID</h4>
            <p class="text-sm text-gray-600">Get your unique 5-character Player ID linked to your IC number</p>
          </div>
          <div class="text-center">
            <div class="bg-green-100 w-16 h-16 rounded-full flex items-center justify-center mx-auto mb-4">
              <i class="fas fa-trophy text-2xl text-green-600"></i>
            </div>
            <h4 class="font-semibold text-gray-800 mb-2">Tournament Access</h4>
            <p class="text-sm text-gray-600">Register for official tournaments and competitions</p>
          </div>
          <div class="text-center">
            <div class="bg-purple-100 w-16 h-16 rounded-full flex items-center justify-center mx-auto mb-4">
              <i class="fas fa-chart-line text-2xl text-purple-600"></i>
            </div>
            <h4 class="font-semibold text-gray-800 mb-2">Ranking System</h4>
            <p class="text-sm text-gray-600">Track your progress and national ranking</p>
          </div>
        </div>
      </div>

    </div>
  </main>

  <%- include('../partials/footer') %>
  <script src="/js/script.js"></script>
  <script>
    // File input trigger function
    function triggerFileInput() {
      console.log('Upload area clicked');
      const fileInput = document.getElementById('profilePicture');
      if (fileInput) {
        fileInput.click();
        console.log('File input triggered');
      } else {
        console.error('File input not found');
      }
    }

    // Wait for DOM to be fully loaded
    document.addEventListener('DOMContentLoaded', function() {
      console.log('DOM loaded - initializing file upload');
      
      // Additional event listener for the choose file button
      const chooseFileBtn = document.getElementById('chooseFileBtn');
      const fileInput = document.getElementById('profilePicture');
      
      if (chooseFileBtn && fileInput) {
        console.log('Choose file button and input found');
        
        // Add click event listener to the button
        chooseFileBtn.addEventListener('click', function(e) {
          e.preventDefault();
          e.stopPropagation();
          console.log('Choose File button clicked via event listener');
          fileInput.click();
        });
        
        // Add click event to upload area
        const uploadArea = document.querySelector('.upload-area');
        if (uploadArea) {
          uploadArea.addEventListener('click', function(e) {
            // Only trigger if clicking on the upload area itself, not the button
            if (e.target === uploadArea || uploadArea.contains(e.target) && !chooseFileBtn.contains(e.target)) {
              console.log('Upload area clicked');
              fileInput.click();
            }
          });
        }
      } else {
        console.error('Choose file button or input not found', {
          button: !!chooseFileBtn,
          input: !!fileInput
        });
      }
    });

    // Profile picture preview
    document.getElementById('profilePicture').addEventListener('change', function(e) {
      console.log('Profile picture input changed');
      const file = e.target.files[0];
      console.log('Selected file:', file);
      
      if (file) {
        // Check file size (5MB limit)
        if (file.size > 5 * 1024 * 1024) {
          alert('File size too large. Please choose a file smaller than 5MB.');
          this.value = '';
          return;
        }
        
        // Check file type
        const allowedTypes = ['image/jpeg', 'image/jpg', 'image/png'];
        if (!allowedTypes.includes(file.type)) {
          alert('Invalid file type. Please choose a JPEG, JPG, or PNG image.');
          this.value = '';
          return;
        }
        
        console.log('File is valid, creating preview');
        const reader = new FileReader();
        reader.onload = function(e) {
          console.log('FileReader loaded successfully');
          const preview = document.getElementById('profilePreview');
          const defaultAvatar = document.getElementById('defaultAvatar');
          preview.src = e.target.result;
          preview.style.display = 'block';
          defaultAvatar.style.display = 'none';
          console.log('Preview updated');
        };
        reader.onerror = function(e) {
          console.error('FileReader error:', e);
          alert('Error reading file. Please try again.');
        };
        reader.readAsDataURL(file);
      }
    });

    // Drag and drop functionality - moved to DOMContentLoaded
    document.addEventListener('DOMContentLoaded', function() {
      const uploadArea = document.querySelector('.upload-area');
      
      if (uploadArea) {
        uploadArea.addEventListener('dragover', (e) => {
          e.preventDefault();
          uploadArea.classList.add('drag-over');
        });

        uploadArea.addEventListener('dragleave', () => {
          uploadArea.classList.remove('drag-over');
        });

        uploadArea.addEventListener('drop', (e) => {
          e.preventDefault();
          e.stopPropagation();
          uploadArea.classList.remove('drag-over');
          
          const files = e.dataTransfer.files;
          console.log('Files dropped:', files);
          
          if (files.length > 0) {
            const fileInput = document.getElementById('profilePicture');
            if (fileInput) {
              fileInput.files = files;
              fileInput.dispatchEvent(new Event('change'));
            }
          }
        });
      }
    });

    // Form validation and submission
    document.getElementById('registrationForm').addEventListener('submit', function(e) {
      console.log('Form submitted');
      
      // Check if form is valid
      if (!this.checkValidity()) {
        console.log('Form validation failed');
        e.preventDefault();
        return;
      }
      
      const submitBtn = document.getElementById('submitBtn');
      submitBtn.innerHTML = '<i class="fas fa-spinner fa-spin mr-3"></i>Processing...';
      submitBtn.disabled = true;
      
      // Log form data
      const formData = new FormData(this);
      console.log('Form data:');
      for (let [key, value] of formData.entries()) {
        if (key === 'profilePicture') {
          console.log(key + ':', value.name ? value.name + ' (' + value.size + ' bytes)' : 'No file');
        } else {
          console.log(key + ':', value);
        }
      }
    });

    // IC Number validation with auto-formatting
    let icCheckTimeout;
    let isIcValid = false;
    
    document.getElementById('icNumber').addEventListener('input', function() {
      let icNumber = this.value.trim();
      
      // Auto-format: Convert 12 digits to xxxxxx-xx-xxxx format
      if (/^[0-9]{12}$/.test(icNumber)) {
        icNumber = icNumber.substring(0, 6) + '-' + icNumber.substring(6, 8) + '-' + icNumber.substring(8, 12);
        this.value = icNumber;
      }
      
      // Remove any non-digit and non-dash characters
      icNumber = icNumber.replace(/[^0-9-]/g, '');
      
      // Ensure proper format as user types
      if (icNumber.length > 0 && !/^[0-9-]*$/.test(icNumber)) {
        icNumber = icNumber.replace(/[^0-9-]/g, '');
        this.value = icNumber;
      }
      
      // Auto-format partial input (add dashes at correct positions)
      if (/^[0-9]{6}$/.test(icNumber)) {
        this.value = icNumber + '-';
        icNumber = this.value;
      } else if (/^[0-9]{6}-[0-9]{2}$/.test(icNumber)) {
        this.value = icNumber + '-';
        icNumber = this.value;
      }
      
      const icRegex = /^[0-9]{6}-[0-9]{2}-[0-9]{4}$/;
      
      // Clear previous timeout
      if (icCheckTimeout) {
        clearTimeout(icCheckTimeout);
      }
      
      // Hide all validation messages
      hideAllIcMessages();
      
      // If empty or invalid format, don't check
      if (!icNumber || !icRegex.test(icNumber)) {
        isIcValid = false;
        updateSubmitButton();
        return;
      }
      
      // Show loading message
      showIcMessage('loading');
      
      // Debounce the API call
      icCheckTimeout = setTimeout(() => {
        checkIcAvailability(icNumber);
      }, 500);
    });
    
    // Handle paste events for IC number
    document.getElementById('icNumber').addEventListener('paste', function(e) {
      setTimeout(() => {
        // Trigger the input event after paste to apply formatting
        this.dispatchEvent(new Event('input'));
      }, 10);
    });
    
    async function checkIcAvailability(icNumber) {
      try {
        const response = await fetch(`/api/check-ic/${encodeURIComponent(icNumber)}`);
        const result = await response.json();
        
        hideAllIcMessages();
        
        if (result.available) {
          showIcMessage('available');
          isIcValid = true;
        } else {
          showIcMessage('unavailable', result.message);
          isIcValid = false;
        }
        
        updateSubmitButton();
      } catch (error) {
        console.error('Error checking IC availability:', error);
        hideAllIcMessages();
        showIcMessage('unavailable', 'Error checking IC number availability');
        isIcValid = false;
        updateSubmitButton();
      }
    }
    
    function hideAllIcMessages() {
      document.getElementById('icValidation').classList.add('hidden');
      document.getElementById('icLoading').classList.add('hidden');
      document.getElementById('icAvailable').classList.add('hidden');
      document.getElementById('icUnavailable').classList.add('hidden');
    }
    
    function showIcMessage(type, message = '') {
      document.getElementById('icValidation').classList.remove('hidden');
      
      if (type === 'loading') {
        document.getElementById('icLoading').classList.remove('hidden');
      } else if (type === 'available') {
        document.getElementById('icAvailable').classList.remove('hidden');
      } else if (type === 'unavailable') {
        document.getElementById('icErrorMessage').textContent = message;
        document.getElementById('icUnavailable').classList.remove('hidden');
      }
    }
    
    function updateSubmitButton() {
      const submitBtn = document.getElementById('submitBtn');
      if (isIcValid) {
        submitBtn.disabled = false;
        submitBtn.classList.remove('opacity-50', 'cursor-not-allowed');
      } else {
        submitBtn.disabled = true;
        submitBtn.classList.add('opacity-50', 'cursor-not-allowed');
      }
    }

    // Username availability check (placeholder for future implementation)
    document.getElementById('username').addEventListener('blur', function() {
      // Could add AJAX call to check username availability
      console.log('Username field blurred:', this.value);
    });

    // Modal functions
    function openTermsModal() {
      console.log('openTermsModal function called');
      const modal = document.getElementById('termsModal');
      if (modal) {
        console.log('Terms modal element found, showing modal');
        modal.classList.remove('hidden');
        modal.style.display = 'flex';
        modal.style.alignItems = 'center';
        modal.style.justifyContent = 'center';
        modal.style.visibility = 'visible';
        modal.style.opacity = '1';
        document.body.style.overflow = 'hidden';
        console.log('Terms modal should now be visible');
      } else {
        console.error('Terms modal element not found!');
      }
    }

    function openPrivacyModal() {
      console.log('openPrivacyModal function called');
      const modal = document.getElementById('privacyModal');
      if (modal) {
        console.log('Privacy modal element found, showing modal');
        modal.classList.remove('hidden');
        modal.style.display = 'flex';
        modal.style.alignItems = 'center';
        modal.style.justifyContent = 'center';
        modal.style.visibility = 'visible';
        modal.style.opacity = '1';
        document.body.style.overflow = 'hidden';
        console.log('Privacy modal should now be visible');
      } else {
        console.error('Privacy modal element not found!');
      }
    }

    function closeModal(modalId) {
      console.log('closeModal function called for:', modalId);
      
      // Get all elements with this ID (there might be duplicates)
      const allModals = document.querySelectorAll('#' + modalId);
      console.log('Number of elements with ID ' + modalId + ':', allModals.length);
      
      if (allModals.length > 0) {
        // Hide all elements with this ID using the most aggressive approach
        allModals.forEach((modal, index) => {
          console.log('Processing modal element', index);
          
          // Method 1: CSS classes and inline styles
          modal.classList.add('hidden');
          modal.setAttribute('style', 'display: none !important; visibility: hidden !important; opacity: 0 !important; z-index: -9999 !important; position: fixed !important; top: -9999px !important; left: -9999px !important;');
          
          // Method 2: Remove from DOM temporarily
          const parent = modal.parentNode;
          if (parent) {
            modal.style.display = 'none';
            modal.style.visibility = 'hidden';
            modal.style.opacity = '0';
            console.log('Modal', index, 'should be hidden now');
          }
        });
        
        document.body.style.overflow = 'auto';
        console.log('All modals with ID', modalId, 'processed');
        
        // Final check - look for ANY visible modal-like elements
        setTimeout(() => {
          const stillVisible = document.querySelectorAll('#' + modalId + ':not(.hidden)');
          console.log('Still visible modals after close:', stillVisible.length);
          stillVisible.forEach(modal => {
            modal.remove(); // Nuclear option - remove from DOM completely
            console.log('Removed stubborn modal from DOM');
          });
          
          // Also check for any elements that might be modal overlays
          const allFixedElements = document.querySelectorAll('div[style*="fixed"], div[style*="z-index"], .fixed');
          console.log('All fixed positioned elements found:', allFixedElements.length);
          
          allFixedElements.forEach((element, index) => {
            const style = window.getComputedStyle(element);
            if (style.position === 'fixed' && 
                (style.backgroundColor.includes('rgba(0, 0, 0') || style.backgroundColor.includes('rgb(0, 0, 0')) &&
                style.zIndex > 1000) {
              console.log('Found potential modal overlay element', index, element);
              element.style.display = 'none !important';
              element.remove();
              console.log('Removed potential modal overlay');
            }
          });
          
          // Nuclear option: hide ALL elements with high z-index that look like modals
          const highZElements = Array.from(document.querySelectorAll('*')).filter(el => {
            const style = window.getComputedStyle(el);
            return parseInt(style.zIndex) > 9000 && style.position === 'fixed';
          });
          
          console.log('High z-index fixed elements:', highZElements.length);
          highZElements.forEach((element, index) => {
            console.log('Hiding high z-index element', index, element.id || element.className);
            element.style.display = 'none !important';
          });
          
        }, 100);
        
      } else {
        console.error('No modal found with ID:', modalId);
      }
    }

    // Initialize
    document.addEventListener('DOMContentLoaded', function() {
      console.log('Player registration form loaded');
      updateSubmitButton(); // Initially disable submit button
      
      // Debug: Check if elements exist
      const termsLink = document.getElementById('termsLink');
      const privacyLink = document.getElementById('privacyLink');
      const termsModal = document.getElementById('termsModal');
      const privacyModal = document.getElementById('privacyModal');
      
      console.log('Terms link found:', !!termsLink);
      console.log('Privacy link found:', !!privacyLink);
      console.log('Terms modal found:', !!termsModal);
      console.log('Privacy modal found:', !!privacyModal);
      
      // Terms modal event listeners
      if (termsLink) {
        console.log('Adding click listener to terms link');
        termsLink.addEventListener('click', function(e) {
          console.log('Terms link clicked!');
          e.preventDefault();
          openTermsModal();
        });
      } else {
        console.error('Terms link not found!');
      }
      
      const termsCloseBtn = document.getElementById('termsCloseBtn');
      const termsCloseFooterBtn = document.getElementById('termsCloseFooterBtn');
      
      console.log('Terms close button found:', !!termsCloseBtn);
      console.log('Terms footer close button found:', !!termsCloseFooterBtn);
      
      if (termsCloseBtn) {
        termsCloseBtn.addEventListener('click', function(e) {
          console.log('Terms close button clicked');
          e.preventDefault();
          e.stopPropagation();
          closeModal('termsModal');
        });
      }
      
      if (termsCloseFooterBtn) {
        termsCloseFooterBtn.addEventListener('click', function(e) {
          console.log('Terms footer close button clicked');
          e.preventDefault();
          e.stopPropagation();
          closeModal('termsModal');
        });
      }
      
      // Privacy modal event listeners
      if (privacyLink) {
        console.log('Adding click listener to privacy link');
        privacyLink.addEventListener('click', function(e) {
          console.log('Privacy link clicked!');
          e.preventDefault();
          openPrivacyModal();
        });
      } else {
        console.error('Privacy link not found!');
      }
      
      const privacyCloseBtn = document.getElementById('privacyCloseBtn');
      const privacyCloseFooterBtn = document.getElementById('privacyCloseFooterBtn');
      
      console.log('Privacy close button found:', !!privacyCloseBtn);
      console.log('Privacy footer close button found:', !!privacyCloseFooterBtn);
      
      if (privacyCloseBtn) {
        privacyCloseBtn.addEventListener('click', function(e) {
          console.log('Privacy close button clicked');
          e.preventDefault();
          e.stopPropagation();
          closeModal('privacyModal');
        });
      }
      
      if (privacyCloseFooterBtn) {
        privacyCloseFooterBtn.addEventListener('click', function(e) {
          console.log('Privacy footer close button clicked');
          e.preventDefault();
          e.stopPropagation();
          closeModal('privacyModal');
        });
      }
      
      // Close modals when clicking outside
      if (termsModal) {
        termsModal.addEventListener('click', function(e) {
          if (e.target === termsModal) {
            console.log('Closing terms modal by outside click');
            closeModal('termsModal');
          }
        });
      }
      
      if (privacyModal) {
        privacyModal.addEventListener('click', function(e) {
          if (e.target === privacyModal) {
            console.log('Closing privacy modal by outside click');
            closeModal('privacyModal');
          }
        });
      }
      
      // Alternative event delegation approach for close buttons
      document.addEventListener('click', function(e) {
        if (e.target.id === 'termsCloseBtn' || e.target.id === 'termsCloseFooterBtn') {
          console.log('Terms close button clicked via delegation');
          e.preventDefault();
          e.stopPropagation();
          closeModal('termsModal');
        }
        
        if (e.target.id === 'privacyCloseBtn' || e.target.id === 'privacyCloseFooterBtn') {
          console.log('Privacy close button clicked via delegation');
          e.preventDefault();
          e.stopPropagation();
          closeModal('privacyModal');
        }
        
        // Also check if the clicked element is inside a close button
        const closestCloseBtn = e.target.closest('#termsCloseBtn, #termsCloseFooterBtn, #privacyCloseBtn, #privacyCloseFooterBtn');
        if (closestCloseBtn) {
          console.log('Close button clicked via closest:', closestCloseBtn.id);
          e.preventDefault();
          e.stopPropagation();
          
          if (closestCloseBtn.id.includes('terms')) {
            closeModal('termsModal');
          } else if (closestCloseBtn.id.includes('privacy')) {
            closeModal('privacyModal');
          }
        }
      });
    });
  </script>

  <!-- Terms and Conditions Modal -->
  <div id="termsModal" class="hidden fixed inset-0 bg-black bg-opacity-50 p-4" style="z-index: 9999; display: none;">
    <div class="bg-white rounded-lg max-w-4xl w-full overflow-hidden" style="margin: auto; max-height: 90vh; display: flex; flex-direction: column;">
      <div class="flex justify-between items-center p-6 border-b">
        <h2 class="text-2xl font-bold text-gray-800">
          <i class="fas fa-file-contract text-blue-600 mr-2"></i>
          Terms and Conditions
        </h2>
        <button id="termsCloseBtn" class="text-gray-500 hover:text-gray-700 text-2xl">
          <i class="fas fa-times"></i>
        </button>
      </div>
      <div class="p-6 overflow-y-auto text-sm leading-relaxed" style="flex: 1; min-height: 0;">
        <div class="space-y-6">
          <div>
            <h3 class="text-lg font-semibold text-blue-600 mb-3">1. Acceptance of Terms</h3>
            <p>By registering as a player with the Malaysia Pickleball Association (MPA), you acknowledge that you have read, understood, and agree to be bound by these Terms and Conditions.</p>
          </div>

          <div>
            <h3 class="text-lg font-semibold text-blue-600 mb-3">2. Membership and Registration</h3>
            <h4 class="font-semibold mb-2">2.1 Eligibility</h4>
            <ul class="list-disc pl-6 space-y-1">
              <li>You must be at least 13 years old to register independently. Players under 18 require parental/guardian consent.</li>
              <li>You must provide accurate and complete information during registration.</li>
              <li>Each person may only maintain one active membership account.</li>
              <li>You must be a Malaysian citizen or permanent resident, or have valid documentation to participate in Malaysian sports activities.</li>
            </ul>
            
            <h4 class="font-semibold mb-2 mt-4">2.2 Registration Process</h4>
            <ul class="list-disc pl-6 space-y-1">
              <li>All registrations are subject to approval by MPA administrators.</li>
              <li>MPA reserves the right to reject any registration without providing specific reasons.</li>
              <li>Upon approval, you will receive a unique MPA Player ID.</li>
              <li>Registration is free of charge, but participation in tournaments may incur fees.</li>
            </ul>
          </div>

          <div>
            <h3 class="text-lg font-semibold text-blue-600 mb-3">3. Player Obligations and Conduct</h3>
            <p>As an MPA registered player, you agree to:</p>
            <ul class="list-disc pl-6 space-y-1 mt-2">
              <li>Conduct yourself in a manner that upholds the integrity and reputation of pickleball and MPA.</li>
              <li>Show respect for opponents, officials, volunteers, and spectators.</li>
              <li>Comply with all rules and regulations of pickleball as established by the International Federation of Pickleball (IFP) and MPA.</li>
              <li>Participate in a spirit of fair play and sportsmanship.</li>
              <li>Refrain from any form of discrimination, harassment, or inappropriate behavior.</li>
            </ul>
          </div>

          <div>
            <h3 class="text-lg font-semibold text-blue-600 mb-3">4. Tournaments and Competitions</h3>
            <ul class="list-disc pl-6 space-y-1">
              <li>Registration as an MPA player does not guarantee entry into tournaments.</li>
              <li>Tournament entries are subject to separate registration processes and fees.</li>
              <li>Players must meet specific eligibility criteria for different tournament categories.</li>
              <li>MPA maintains a ranking system based on tournament performance.</li>
            </ul>
          </div>

          <div>
            <h3 class="text-lg font-semibold text-blue-600 mb-3">5. Liability and Insurance</h3>
            <p><strong>Assumption of Risk:</strong> By participating in pickleball activities, you acknowledge that:</p>
            <ul class="list-disc pl-6 space-y-1 mt-2">
              <li>Pickleball involves inherent risks of injury.</li>
              <li>You participate at your own risk and assume full responsibility for any injuries.</li>
              <li>MPA is not liable for injuries, accidents, or damages that may occur during participation.</li>
              <li>Players are strongly encouraged to maintain personal health and accident insurance.</li>
            </ul>
          </div>

          <div>
            <h3 class="text-lg font-semibold text-blue-600 mb-3">6. Disciplinary Actions</h3>
            <p>MPA may take disciplinary action for violations including breach of code of conduct, unsportsmanlike behavior, or providing false information. Penalties may include warnings, suspension, or termination of membership.</p>
          </div>

          <div>
            <h3 class="text-lg font-semibold text-blue-600 mb-3">7. Governing Law</h3>
            <p>These Terms and Conditions are governed by the laws of Malaysia. Any disputes arising from these terms shall be subject to the jurisdiction of Malaysian courts.</p>
          </div>

          <div class="bg-blue-50 p-4 rounded-lg border-l-4 border-blue-500">
            <p class="text-blue-800"><strong>Important:</strong> By completing your registration, you acknowledge that you have read, understood, and agree to abide by these Terms and Conditions.</p>
          </div>
        </div>
      </div>
      <div class="p-6 border-t bg-gray-50" style="flex-shrink: 0;">
        <button id="termsCloseFooterBtn" class="bg-blue-600 text-white px-6 py-2 rounded-lg hover:bg-blue-700 transition-colors">
          Close
        </button>
      </div>
    </div>
  </div>

  <!-- Privacy Policy Modal -->
  <div id="privacyModal" class="hidden fixed inset-0 bg-black bg-opacity-50 p-4" style="z-index: 9999; display: none;">
    <div class="bg-white rounded-lg max-w-4xl w-full overflow-hidden" style="margin: auto; max-height: 90vh; display: flex; flex-direction: column;">
      <div class="flex justify-between items-center p-6 border-b">
        <h2 class="text-2xl font-bold text-gray-800">
          <i class="fas fa-shield-alt text-green-600 mr-2"></i>
          Privacy Policy
        </h2>
        <button id="privacyCloseBtn" class="text-gray-500 hover:text-gray-700 text-2xl">
          <i class="fas fa-times"></i>
        </button>
      </div>
      <div class="p-6 overflow-y-auto text-sm leading-relaxed" style="flex: 1; min-height: 0;">
        <div class="space-y-6">
          <div>
            <h3 class="text-lg font-semibold text-green-600 mb-3">1. Information We Collect</h3>
            <p>When you register as a player with MPA, we collect:</p>
            <ul class="list-disc pl-6 space-y-1 mt-2">
              <li><strong>Identity Information:</strong> Full name, IC number, age, date of birth</li>
              <li><strong>Contact Information:</strong> Email address, phone number, address, city, postcode</li>
              <li><strong>Account Information:</strong> Username, password (encrypted)</li>
              <li><strong>Profile Information:</strong> Profile picture, playing preferences</li>
              <li><strong>Tournament Data:</strong> Match results, rankings, statistics</li>
            </ul>
          </div>

          <div>
            <h3 class="text-lg font-semibold text-green-600 mb-3">2. How We Use Your Information</h3>
            <ul class="list-disc pl-6 space-y-1">
              <li><strong>Player Registration:</strong> Processing and managing your membership</li>
              <li><strong>Tournament Management:</strong> Entry processing, scheduling, and results management</li>
              <li><strong>Communication:</strong> Sending updates, announcements, and important notices</li>
              <li><strong>Rankings:</strong> Calculating and maintaining player rankings and statistics</li>
              <li><strong>Event Organization:</strong> Planning and coordinating tournaments and activities</li>
            </ul>
          </div>

          <div>
            <h3 class="text-lg font-semibold text-green-600 mb-3">3. Data Sharing and Disclosure</h3>
            <h4 class="font-semibold mb-2">Within MPA:</h4>
            <ul class="list-disc pl-6 space-y-1">
              <li>MPA administrators and authorized staff</li>
              <li>Tournament directors and organizers</li>
              <li>Certified referees and officials</li>
            </ul>
            
            <h4 class="font-semibold mb-2 mt-4">Public Information:</h4>
            <ul class="list-disc pl-6 space-y-1">
              <li>Player name and MPA ID</li>
              <li>Tournament results and rankings</li>
              <li>Achievement records and statistics</li>
            </ul>
          </div>

          <div>
            <h3 class="text-lg font-semibold text-green-600 mb-3">4. Data Security</h3>
            <ul class="list-disc pl-6 space-y-1">
              <li><strong>Encryption:</strong> All sensitive data is encrypted in transit and at rest</li>
              <li><strong>Access Controls:</strong> Role-based access with authentication requirements</li>
              <li><strong>Regular Backups:</strong> Secure backup systems with disaster recovery plans</li>
              <li><strong>Security Monitoring:</strong> Continuous monitoring for unauthorized access</li>
            </ul>
          </div>

          <div>
            <h3 class="text-lg font-semibold text-green-600 mb-3">5. Your Rights</h3>
            <p>Under the Malaysian PDPA, you have the right to:</p>
            <ul class="list-disc pl-6 space-y-1 mt-2">
              <li>Request a copy of your personal data we hold</li>
              <li>Update or correct inaccurate personal information</li>
              <li>Request deletion of your personal data (subject to legal requirements)</li>
              <li>Withdraw consent for processing (where applicable)</li>
              <li>Object to processing for direct marketing</li>
            </ul>
          </div>

          <div>
            <h3 class="text-lg font-semibold text-green-600 mb-3">6. Data Retention</h3>
            <ul class="list-disc pl-6 space-y-1">
              <li><strong>Active Player Records:</strong> Duration of membership + 2 years</li>
              <li><strong>Tournament Results:</strong> Permanent (anonymized after 10 years)</li>
              <li><strong>Communication Records:</strong> 3 years</li>
              <li><strong>Website Logs:</strong> 1 year</li>
            </ul>
          </div>

          <div>
            <h3 class="text-lg font-semibold text-green-600 mb-3">7. Children's Privacy</h3>
            <ul class="list-disc pl-6 space-y-1">
              <li>Players under 18 require parental/guardian consent</li>
              <li>We take extra care to protect children's personal information</li>
              <li>Parents can request access to or deletion of their child's data</li>
            </ul>
          </div>

          <div>
            <h3 class="text-lg font-semibold text-green-600 mb-3">8. Contact Information</h3>
            <div class="bg-gray-50 p-4 rounded-lg">
              <p><strong>Data Protection Officer</strong></p>
              <p>Email: privacy@malaysiapickleball.org</p>
              <p>Phone: +60 3-XXXX XXXX</p>
            </div>
          </div>

          <div class="bg-green-50 p-4 rounded-lg border-l-4 border-green-500">
            <p class="text-green-800"><strong>Your Privacy Matters:</strong> We are committed to protecting your personal information and will only use it as described in this policy.</p>
          </div>
        </div>
      </div>
      <div class="p-6 border-t bg-gray-50" style="flex-shrink: 0;">
        <button id="privacyCloseFooterBtn" class="bg-green-600 text-white px-6 py-2 rounded-lg hover:bg-green-700 transition-colors">
          Close
        </button>
      </div>
    </div>
  </div>

</body>
</html> 