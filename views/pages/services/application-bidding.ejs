<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Bidding Application - Malaysia Pickleball Association</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/styles.css">
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
  <style>
    .bidding-hero {
      background: linear-gradient(135deg, #7c3aed 0%, #5b21b6 50%, #4c1d95 100%);
    }
    
    .interactive-card {
      transition: all 0.4s cubic-bezier(0.4, 0, 0.2, 1);
      background: rgba(255, 255, 255, 0.95);
      backdrop-filter: blur(15px);
      border: 1px solid rgba(255, 255, 255, 0.2);
    }
    
    .interactive-card:hover {
      transform: translateY(-8px) scale(1.01);
      box-shadow: 0 25px 50px rgba(0, 0, 0, 0.15);
      background: rgba(255, 255, 255, 0.98);
    }
    
    .step-card {
      position: relative;
      overflow: hidden;
      transition: all 0.5s ease;
    }
    
    .step-card::before {
      content: '';
      position: absolute;
      top: 0;
      left: -100%;
      width: 100%;
      height: 100%;
      background: linear-gradient(90deg, transparent, rgba(124, 58, 237, 0.1), transparent);
      transition: left 0.6s;
    }
    
    .step-card:hover::before {
      left: 100%;
    }
    
    .step-number {
      position: relative;
      z-index: 10;
    }
    
    .timeline-connector {
      position: relative;
    }
    
    .timeline-connector::after {
      content: '';
      position: absolute;
      top: 50%;
      right: -2rem;
      width: 4rem;
      height: 2px;
      background: linear-gradient(90deg, #7c3aed, #a855f7);
      transform: translateY(-50%);
    }
    
    .timeline-connector:last-child::after {
      display: none;
    }
    
    .feature-icon {
      transition: all 0.4s ease;
    }
    
    .feature-card:hover .feature-icon {
      transform: scale(1.2) rotate(10deg);
    }
    
    .feature-card {
      transition: all 0.4s ease;
      cursor: pointer;
    }
    
    .feature-card:hover {
      transform: translateY(-10px);
      box-shadow: 0 20px 40px rgba(124, 58, 237, 0.2);
    }
    
    .cta-section {
      background: linear-gradient(135deg, #f3e8ff, #e9d5ff, #ddd6fe);
      position: relative;
      overflow: hidden;
    }
    
    .cta-section::before {
      content: '';
      position: absolute;
      top: -50%;
      left: -50%;
      width: 200%;
      height: 200%;
      background: radial-gradient(circle, rgba(124, 58, 237, 0.1) 0%, transparent 70%);
      animation: rotate 20s linear infinite;
    }
    
    @keyframes rotate {
      from { transform: rotate(0deg); }
      to { transform: rotate(360deg); }
    }
    
    .floating-icon {
      animation: float 3s ease-in-out infinite;
    }
    
    .floating-icon:nth-child(2n) {
      animation-delay: -1.5s;
    }
    
    @keyframes float {
      0%, 100% { transform: translateY(0px); }
      50% { transform: translateY(-10px); }
    }
    
    .progress-bar {
      height: 6px;
      background: linear-gradient(90deg, #7c3aed, #a855f7, #c084fc);
      border-radius: 3px;
      animation: progressPulse 2s ease-in-out infinite;
    }
    
    @keyframes progressPulse {
      0%, 100% { opacity: 0.8; }
      50% { opacity: 1; }
    }
    
    .fade-in {
      opacity: 0;
      transform: translateY(40px);
      animation: fadeInUp 0.8s ease-out forwards;
    }
    
    .fade-in-delay {
      opacity: 0;
      transform: translateY(40px);
      animation: fadeInUp 0.8s ease-out 0.4s forwards;
    }
    
    .fade-in-delay-2 {
      opacity: 0;
      transform: translateY(40px);
      animation: fadeInUp 0.8s ease-out 0.8s forwards;
    }
    
    @keyframes fadeInUp {
      to {
        opacity: 1;
        transform: translateY(0);
      }
    }
    
    @media (max-width: 768px) {
      .timeline-connector::after {
        display: none;
      }
      
      .step-card {
        margin-bottom: 2rem;
      }
      
      .hero-title {
        font-size: 2rem !important;
      }
    }
  </style>
</head>
<%
  const defaultBg = backgroundImage || '/images/defaultbg.png';
%>
<body class="bg-gray-100 bg-cover bg-center bg-fixed min-h-screen" style="background-image: url('<%= defaultBg %>')">
  <%- include('../../partials/header') %>

<main class="pt-24 sm:pt-28 lg:pt-32 p-4 pb-20 min-h-screen">
    <div class="container mx-auto max-w-7xl">
      <!-- Hero Section -->
      <div class="bidding-hero text-white p-10 rounded-3xl shadow-2xl mb-12 text-center relative fade-in">
        <div class="absolute top-4 right-4 floating-icon">
          <i class="fas fa-trophy text-yellow-300 text-2xl"></i>
        </div>
        <div class="absolute top-8 left-8 floating-icon">
          <i class="fas fa-medal text-yellow-200 text-xl"></i>
        </div>
        <div class="absolute bottom-6 right-12 floating-icon">
          <i class="fas fa-crown text-yellow-400 text-lg"></i>
        </div>
        
        <div class="inline-flex items-center justify-center w-24 h-24 bg-white bg-opacity-20 rounded-full mb-8">
          <i class="fas fa-gavel text-white text-4xl"></i>
        </div>
        <h1 class="hero-title text-4xl md:text-6xl font-bold mb-6">
          Win the Bid
        </h1>
        <p class="text-xl md:text-2xl mb-8 opacity-90 max-w-4xl mx-auto">
          Secure your opportunity to organize prestigious sports events. Master the bidding process and stand out from the competition.
        </p>
        <div class="progress-bar w-full max-w-md mx-auto mb-8"></div>
        <div class="flex flex-wrap justify-center gap-4">
          <button class="bg-white text-purple-600 px-8 py-4 rounded-xl font-bold text-lg hover:bg-opacity-90 transition-all duration-300 transform hover:scale-105" onclick="scrollToSection('process')">
            <i class="fas fa-rocket mr-2"></i>
            Start Bidding
          </button>
          <button class="border-2 border-white text-white px-8 py-4 rounded-xl font-bold text-lg hover:bg-white hover:text-purple-600 transition-all duration-300" onclick="scrollToSection('requirements')">
            <i class="fas fa-checklist mr-2"></i>
            View Requirements
          </button>
        </div>
      </div>

      <!-- Bidding Process Timeline -->
      <section id="process" class="mb-16 fade-in-delay">
        <h2 class="text-4xl font-bold text-center mb-12 text-gray-800">
          <i class="fas fa-road text-purple-600 mr-4"></i>
          Bidding Journey
        </h2>
        
        <div class="grid grid-cols-1 md:grid-cols-4 gap-8">
          <!-- Step 1 -->
          <div class="step-card interactive-card p-8 rounded-2xl text-center timeline-connector">
            <div class="step-number inline-flex items-center justify-center w-16 h-16 bg-gradient-to-r from-purple-500 to-purple-600 rounded-full mb-6 text-white text-2xl font-bold">
              1
            </div>
            <div class="feature-icon mb-4">
              <i class="fas fa-search text-purple-600 text-3xl"></i>
            </div>
            <h3 class="text-xl font-bold mb-4 text-gray-800">Research & Prepare</h3>
            <p class="text-gray-600">Analyze requirements, study competition, and prepare your comprehensive bid proposal.</p>
          </div>

          <!-- Step 2 -->
          <div class="step-card interactive-card p-8 rounded-2xl text-center timeline-connector">
            <div class="step-number inline-flex items-center justify-center w-16 h-16 bg-gradient-to-r from-blue-500 to-blue-600 rounded-full mb-6 text-white text-2xl font-bold">
              2
            </div>
            <div class="feature-icon mb-4">
              <i class="fas fa-file-alt text-blue-600 text-3xl"></i>
            </div>
            <h3 class="text-xl font-bold mb-4 text-gray-800">Submit Proposal</h3>
            <p class="text-gray-600">Submit your detailed bidding proposal with all required documentation and supporting materials.</p>
          </div>

          <!-- Step 3 -->
          <div class="step-card interactive-card p-8 rounded-2xl text-center timeline-connector">
            <div class="step-number inline-flex items-center justify-center w-16 h-16 bg-gradient-to-r from-green-500 to-green-600 rounded-full mb-6 text-white text-2xl font-bold">
              3
            </div>
            <div class="feature-icon mb-4">
              <i class="fas fa-users text-green-600 text-3xl"></i>
            </div>
            <h3 class="text-xl font-bold mb-4 text-gray-800">Evaluation & Review</h3>
            <p class="text-gray-600">Expert panel evaluates all proposals based on criteria including capability, experience, and resources.</p>
          </div>

          <!-- Step 4 -->
          <div class="step-card interactive-card p-8 rounded-2xl text-center">
            <div class="step-number inline-flex items-center justify-center w-16 h-16 bg-gradient-to-r from-yellow-500 to-orange-500 rounded-full mb-6 text-white text-2xl font-bold">
              4
            </div>
            <div class="feature-icon mb-4">
              <i class="fas fa-trophy text-yellow-600 text-3xl"></i>
            </div>
            <h3 class="text-xl font-bold mb-4 text-gray-800">Award & Execute</h3>
            <p class="text-gray-600">Successful bidder receives approval and begins event organization with official backing.</p>
          </div>
        </div>
      </section>

      <!-- Key Features -->
      <section id="requirements" class="mb-16 fade-in-delay-2">
        <h2 class="text-4xl font-bold text-center mb-12 text-gray-800">
          <i class="fas fa-star text-purple-600 mr-4"></i>
          Why Choose Our Platform?
        </h2>
        
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8">
          <!-- Competitive Edge -->
          <div class="feature-card interactive-card p-8 rounded-2xl text-center">
            <div class="feature-icon inline-flex items-center justify-center w-20 h-20 bg-gradient-to-r from-purple-100 to-purple-200 rounded-full mb-6">
              <i class="fas fa-chart-line text-purple-600 text-3xl"></i>
            </div>
            <h3 class="text-2xl font-bold mb-4 text-gray-800">Competitive Edge</h3>
            <p class="text-gray-600 mb-4">Gain advantage with our proven bidding strategies and expert guidance.</p>
            <div class="bg-purple-50 p-4 rounded-lg">
              <p class="text-purple-700 font-semibold">95% Success Rate</p>
            </div>
          </div>

          <!-- Official Recognition -->
          <div class="feature-card interactive-card p-8 rounded-2xl text-center">
            <div class="feature-icon inline-flex items-center justify-center w-20 h-20 bg-gradient-to-r from-green-100 to-green-200 rounded-full mb-6">
              <i class="fas fa-certificate text-green-600 text-3xl"></i>
            </div>
            <h3 class="text-2xl font-bold mb-4 text-gray-800">Official Recognition</h3>
            <p class="text-gray-600 mb-4">Government-backed authorization and full legal compliance assurance.</p>
            <div class="bg-green-50 p-4 rounded-lg">
              <p class="text-green-700 font-semibold">100% Legal Compliance</p>
            </div>
          </div>

          <!-- Expert Support -->
          <div class="feature-card interactive-card p-8 rounded-2xl text-center">
            <div class="feature-icon inline-flex items-center justify-center w-20 h-20 bg-gradient-to-r from-blue-100 to-blue-200 rounded-full mb-6">
              <i class="fas fa-headset text-blue-600 text-3xl"></i>
            </div>
            <h3 class="text-2xl font-bold mb-4 text-gray-800">Expert Support</h3>
            <p class="text-gray-600 mb-4">Dedicated team of professionals to guide you through every step.</p>
            <div class="bg-blue-50 p-4 rounded-lg">
              <p class="text-blue-700 font-semibold">24/7 Assistance</p>
            </div>
          </div>
        </div>
      </section>

      <!-- Call to Action -->
      <section class="cta-section p-12 rounded-3xl mb-12 text-center relative fade-in-delay-2">
        <div class="relative z-10">
          <h2 class="text-4xl md:text-5xl font-bold mb-8 text-gray-800">
            <i class="fas fa-bullseye text-purple-600 mr-4"></i>
            Ready to Win?
          </h2>
          <p class="text-xl md:text-2xl text-gray-700 mb-10 max-w-3xl mx-auto">
            Transform your sports event vision into reality. Join the ranks of successful event organizers who chose our platform.
          </p>
          
          <div class="grid grid-cols-1 md:grid-cols-3 gap-8 mb-10">
            <div class="text-center">
              <div class="inline-flex items-center justify-center w-20 h-20 bg-white rounded-full mb-4 shadow-lg">
                <i class="fas fa-lightning-bolt text-purple-600 text-3xl"></i>
              </div>
              <h4 class="text-xl font-bold text-gray-800 mb-2">Fast Track</h4>
              <p class="text-gray-600">Expedited processing for qualified bidders</p>
            </div>
            
            <div class="text-center">
              <div class="inline-flex items-center justify-center w-20 h-20 bg-white rounded-full mb-4 shadow-lg">
                <i class="fas fa-shield-check text-green-600 text-3xl"></i>
              </div>
              <h4 class="text-xl font-bold text-gray-800 mb-2">Guaranteed Process</h4>
              <p class="text-gray-600">Transparent and fair evaluation system</p>
            </div>
            
            <div class="text-center">
              <div class="inline-flex items-center justify-center w-20 h-20 bg-white rounded-full mb-4 shadow-lg">
                <i class="fas fa-handshake text-blue-600 text-3xl"></i>
              </div>
              <h4 class="text-xl font-bold text-gray-800 mb-2">Partnership</h4>
              <p class="text-gray-600">Long-term support beyond winning the bid</p>
            </div>
          </div>
          
          <div class="flex flex-wrap justify-center gap-6">
            <a href="http://erosa.kbs.gov.my" target="_blank" class="inline-flex items-center bg-gradient-to-r from-purple-600 to-purple-700 text-white px-10 py-5 rounded-2xl font-bold text-xl hover:from-purple-700 hover:to-purple-800 transition-all duration-300 transform hover:scale-105 shadow-lg">
              <i class="fas fa-rocket mr-3"></i>
              Submit Your Bid Now
            </a>
            <button class="inline-flex items-center bg-white text-purple-600 px-10 py-5 rounded-2xl font-bold text-xl hover:bg-gray-50 transition-all duration-300 shadow-lg" onclick="downloadBiddingGuide()">
              <i class="fas fa-download mr-3"></i>
              Download Bidding Guide
            </button>
          </div>
        </div>
      </section>

      <!-- Infographic Display -->
      <section class="interactive-card p-10 rounded-3xl text-center mb-12 fade-in-delay-2">
        <h2 class="text-4xl font-bold mb-8 text-gray-800">
          <i class="fas fa-map text-purple-600 mr-4"></i>
          Bidding Process Roadmap
        </h2>
        <div class="overflow-hidden rounded-2xl shadow-2xl">
          <img src="/images/pic5.png" alt="Bidding Application Infographic" class="w-full h-auto transition-transform duration-700 hover:scale-110">
        </div>
        <div class="mt-8">
          <p class="text-lg text-gray-600 mb-4">Your comprehensive guide to successful event bidding</p>
          <div class="flex justify-center gap-4">
            <span class="bg-purple-100 text-purple-700 px-4 py-2 rounded-full text-sm font-semibold">
              <i class="fas fa-eye mr-2"></i>
              Click to zoom
            </span>
            <span class="bg-green-100 text-green-700 px-4 py-2 rounded-full text-sm font-semibold">
              <i class="fas fa-download mr-2"></i>
              Downloadable
            </span>
          </div>
        </div>
      </section>

      <!-- Government Seal -->
      <div class="text-center mt-12 interactive-card p-8 rounded-2xl shadow-lg">
        <div class="flex items-center justify-center mb-4">
          <i class="fas fa-flag text-red-500 text-3xl mr-4"></i>
          <span class="text-2xl font-bold text-gray-800">Government of Malaysia</span>
        </div>
        <div class="flex items-center justify-center text-red-600">
          <i class="fas fa-heart mr-2 floating-icon"></i>
          <span class="text-xl font-semibold">Madani</span>
        </div>
        <div class="mt-4 text-sm text-gray-600">
          <p>Official Sports Development Authority • Act 576 Compliant</p>
        </div>
      </div>
    </div>
  </main>

  <%- include('../../partials/footer') %>
  <script src="/js/script.js"></script>
  <script>
    // Smooth scrolling functionality
    function scrollToSection(sectionId) {
      document.getElementById(sectionId).scrollIntoView({
        behavior: 'smooth',
        block: 'start'
      });
    }
    
    // Download bidding guide
    function downloadBiddingGuide() {
      const link = document.createElement('a');
      link.href = '/images/pic5.png';
      link.download = 'Bidding-Application-Guide.png';
      document.body.appendChild(link);
      link.click();
      document.body.removeChild(link);
      
      // Show confirmation
      const button = event.target.closest('button');
      const originalText = button.innerHTML;
      button.innerHTML = '<i class="fas fa-check mr-3"></i>Downloaded!';
      button.style.background = '#10b981';
      button.style.color = 'white';
      
      setTimeout(() => {
        button.innerHTML = originalText;
        button.style.background = '';
        button.style.color = '';
      }, 2000);
    }
    
    // Intersection Observer for scroll animations
    const observerOptions = {
      threshold: 0.1,
      rootMargin: '0px 0px -50px 0px'
    };

    const observer = new IntersectionObserver((entries) => {
      entries.forEach(entry => {
        if (entry.isIntersecting) {
          entry.target.style.opacity = '1';
          entry.target.style.transform = 'translateY(0)';
        }
      });
    }, observerOptions);

    // Observe all animated elements
    document.querySelectorAll('.fade-in, .fade-in-delay, .fade-in-delay-2, .step-card, .feature-card').forEach(el => {
      observer.observe(el);
    });
    
    // Enhanced hover effects for step cards
    document.querySelectorAll('.step-card').forEach((card, index) => {
      card.addEventListener('mouseenter', () => {
        card.style.transform = 'translateY(-10px) scale(1.02)';
        card.style.zIndex = '10';
      });
      
      card.addEventListener('mouseleave', () => {
        card.style.transform = 'translateY(0) scale(1)';
        card.style.zIndex = '1';
      });
    });
    
    // Feature card interactions
    document.querySelectorAll('.feature-card').forEach(card => {
      card.addEventListener('click', () => {
        // Add a ripple effect
        const ripple = document.createElement('div');
        ripple.style.position = 'absolute';
        ripple.style.background = 'rgba(124, 58, 237, 0.3)';
        ripple.style.borderRadius = '50%';
        ripple.style.transform = 'scale(0)';
        ripple.style.animation = 'ripple 0.6s linear';
        ripple.style.left = '50%';
        ripple.style.top = '50%';
        ripple.style.width = '20px';
        ripple.style.height = '20px';
        ripple.style.marginLeft = '-10px';
        ripple.style.marginTop = '-10px';
        
        card.style.position = 'relative';
        card.appendChild(ripple);
        
        setTimeout(() => {
          ripple.remove();
        }, 600);
      });
    });
    
    // Add ripple animation CSS
    const style = document.createElement('style');
    style.textContent = `
      @keyframes ripple {
        to {
          transform: scale(4);
          opacity: 0;
        }
      }
    `;
    document.head.appendChild(style);
    
    // Progress bar animation trigger
    window.addEventListener('scroll', () => {
      const progressBars = document.querySelectorAll('.progress-bar');
      progressBars.forEach(bar => {
        const rect = bar.getBoundingClientRect();
        if (rect.top < window.innerHeight && rect.bottom > 0) {
          bar.style.animation = 'progressPulse 2s ease-in-out infinite';
        }
      });
    });
  </script>
</body>
</html>
