<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Manage Quick Stats - Malaysia Pickleball Association</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/styles.css">
</head>
<body class="bg-gray-100">
  <main class="p-6">
    <div class="container mx-auto max-w-3xl">
      <div class="flex justify-between items-center mb-6">
        <h1 class="text-3xl font-bold">Manage Quick Stats</h1>
        <a href="/admin/dashboard" class="bg-gray-600 text-white px-4 py-2 rounded hover:bg-gray-700">Back to Dashboard</a>
      </div>

      <% if (typeof window === 'undefined') { %>
      <% } %>

      <div class="bg-white p-6 rounded-lg shadow">
        <form method="POST" action="/admin/home-stats" class="space-y-6">
          <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
            <div>
              <label class="block text-sm font-medium text-gray-700 mb-1">Active Players</label>
              <input type="number" min="0" name="players" value="<%= quickStats?.players ?? 500 %>" class="w-full p-3 border rounded-lg" required>
              <p class="text-xs text-gray-500 mt-1">Shown as value with a trailing + on homepage</p>
            </div>
            <div>
              <label class="block text-sm font-medium text-gray-700 mb-1">Tournaments</label>
              <input type="number" min="0" name="tournaments" value="<%= quickStats?.tournaments ?? 25 %>" class="w-full p-3 border rounded-lg" required>
            </div>
            <div>
              <label class="block text-sm font-medium text-gray-700 mb-1">Venues</label>
              <input type="number" min="0" name="venues" value="<%= quickStats?.venues ?? 15 %>" class="w-full p-3 border rounded-lg" required>
            </div>
            <div>
              <label class="block text-sm font-medium text-gray-700 mb-1">Coaches</label>
              <input type="number" min="0" name="coaches" value="<%= quickStats?.coaches ?? 50 %>" class="w-full p-3 border rounded-lg" required>
            </div>
          </div>

          <div class="flex items-center justify-between pt-4">
            <a href="/" target="_blank" class="text-blue-600 hover:text-blue-800 underline">View on Homepage</a>
            <button type="submit" class="bg-blue-600 text-white px-6 py-3 rounded-lg hover:bg-blue-700">Save Stats</button>
          </div>
        </form>
      </div>
    </div>
  </main>
  <%- include('../../partials/footer') %>
</body>
</html>


