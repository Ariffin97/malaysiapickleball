<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Venue - <%= venue.name %></title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
  <link rel="stylesheet" href="/css/styles.css">
</head>
<body class="bg-gray-100">
  <%- include('../../partials/header') %>
  <main class="pt-20 p-4 pb-20">
    <div class="container mx-auto max-w-3xl">
      <h1 class="text-3xl font-bold mb-4">Edit Venue</h1>
      <form action="/admin/venues/<%= venue._id %>" method="POST" enctype="multipart/form-data" class="bg-white p-4 rounded shadow space-y-4">
        <div>
          <label class="block mb-1">Venue Name</label>
          <input type="text" name="name" class="w-full p-2 border rounded" value="<%= venue.name %>" required>
        </div>
        <div>
          <label class="block mb-1">Address</label>
          <textarea name="address" class="w-full p-2 border rounded" required><%= venue.address %></textarea>
        </div>
        <div class="grid grid-cols-1 md:grid-cols-2 gap-4">
          <div>
            <label class="block mb-1">Booking URL</label>
            <input type="url" name="bookingUrl" class="w-full p-2 border rounded" value="<%= venue.bookingUrl || '' %>">
          </div>
          <div>
            <label class="block mb-1">Add Images</label>
            <input type="file" name="imageFiles" accept="image/*" multiple class="w-full p-2 border rounded bg-white">
          </div>
          <div>
            <label class="block mb-1">Total Courts</label>
            <input type="number" min="0" name="totalCourts" class="w-full p-2 border rounded" value="<%= venue.totalCourts || 0 %>">
          </div>
          <div>
            <label class="block mb-1">Owner</label>
            <input type="text" name="owner" class="w-full p-2 border rounded" value="<%= venue.owner || '' %>">
          </div>
          <div>
            <label class="block mb-1">Phone</label>
            <input type="text" name="phone" class="w-full p-2 border rounded" value="<%= venue.phone || '' %>">
          </div>
          <div>
            <label class="block mb-1">Maps URL</label>
            <input type="url" name="mapsUrl" class="w-full p-2 border rounded" value="<%= venue.mapsUrl || '' %>">
          </div>
          <div class="md:col-span-2">
            <label class="block mb-1">Supported Tournament Levels (comma separated)</label>
            <input type="text" name="tournamentLevels" class="w-full p-2 border rounded" value="<%= (venue.tournamentLevels||[]).join(', ') %>">
          </div>
        </div>
        <div>
          <label class="block mb-1">Description</label>
          <textarea name="description" class="w-full p-2 border rounded" rows="3"><%= venue.description || '' %></textarea>
        </div>
        <div class="pt-2 flex items-center space-x-3">
          <a href="/admin/venues" class="px-4 py-2 bg-gray-600 text-white rounded hover:bg-gray-700">Cancel</a>
          <button type="submit" class="px-4 py-2 bg-blue-600 text-white rounded hover:bg-blue-700">Save Changes</button>
        </div>
      </form>

      <div class="mt-6 bg-white p-4 rounded shadow">
        <h2 class="text-lg font-semibold mb-3">Existing Images</h2>
        <% const imgs = (venue.imageUrls && venue.imageUrls.length) ? venue.imageUrls : (venue.imageUrl ? [venue.imageUrl] : []); %>
        <% if (imgs.length) { %>
          <div class="grid grid-cols-2 md:grid-cols-4 gap-3">
            <% imgs.forEach(function(u){ %>
              <img src="<%= u %>" class="w-full h-32 object-cover rounded" />
            <% }) %>
          </div>
        <% } else { %>
          <p class="text-gray-600">No images yet.</p>
        <% } %>
      </div>
    </div>
  </main>
  <%- include('../../partials/footer') %>
</body>
</html>


