<nav class="bg-green-600 text-white p-4 fixed w-full top-0 z-20 shadow-md">
  <div class="container mx-auto flex justify-between items-center">
    <div class="flex items-center space-x-3">
      <!-- Mobile Sidebar Toggle Button -->
      <button id="mobile-sidebar-toggle" class="md:hidden p-2 rounded-md hover:bg-green-700 focus:outline-none focus:ring-2 focus:ring-white">
        <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16"></path>
        </svg>
      </button>
      <div class="text-lg font-bold">Malaysia Pickleball Association</div>
    </div>
    <div class="flex space-x-4">
      <a href="/" class="hover:underline">Home</a>
      <% if (session.isAuthenticated) { %>
        <a href="/logout" class="hover:underline">Logout</a>
      <% } else { %>
        <a href="/login" class="hover:underline">Login</a>
      <% } %>
    </div>
  </div>
</nav>